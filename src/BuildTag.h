#pragma once
/**
 * Метка версии сборки.
 *
 * ЗАЧЕМ: в тесте билда 21 пришёл лог, который на самом деле был снят
 * с билда 20. Понять это удалось только по формату строк — то есть
 * случайно. Итерация потеряна.
 *
 * Теперь версия печатается первой строкой лога и видна в F12-панели.
 * Правило: поднимать MOD_BUILD_TAG при КАЖДОЙ сборке зипа.
 */

#define MOD_BUILD_TAG "34-pack-fix"
