<?xml version="1.0" encoding="utf-8"?>
<CheatTable CheatEngineTableVersion="46">
  <CheatEntries>
    <CheatEntry>
      <ID>9000</ID>
      <Description>"YAW SCANNER - запусти, поверни персонажа, запусти снова"</Description>
      <VariableType>Auto Assembler Script</VariableType>
      <AssemblerScript>[ENABLE]
aobscanmodule(pBaseSig,DDDA.exe,8B 15 ?? ?? ?? ?? 33 DB 8B F8)

{$lua}
if syntaxcheck then return end
local sig = getAddress('pBaseSig')
if not sig or sig == 0 then print('ERROR: pBaseSig not found'); return end
local pBase = readPointer(readInteger(sig + 2))
if not pBase or pBase == 0 then print('ERROR: pBase = 0'); return end
local player = pBase + 0xA7000
print('--- SCAN ---')
for i = 0, 0x200, 4 do
  local v = readFloat(player + i)
  if v and v ~= 0 and v == v and v > -10000 and v < 10000 then
    print(string.format('+0x%03X = %.4f', i, v))
  end
end
print('--- DONE ---')
{$asm}

[DISABLE]
{$lua}
if syntaxcheck then return end
{$asm}
</AssemblerScript>
    </CheatEntry>
  </CheatEntries>
</CheatTable>
