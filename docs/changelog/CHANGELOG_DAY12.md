# День 12 — 17.08.2026: Build 60 — быстрый поиск позиций + фикс стабильности

## Контекст

Пользователь: после разведки целей — три задачи:
1. починить баг 0,0,0 на холодном старте;
2. почистить спам логов;
3. ускорить скан памяти для поиска тел игрока/пешки («больно долго ищет»).

## Что сделано

### Быстрый census (scan speed)
- `PartyFindBodies(bool partyOnly=false)` — в fast-режиме ранний выход, как
  только найдены uPlayer + uCmc (позициям не нужен полный проход ради
  priority/targetSel). ~в разы быстрее.
- Полный census остался для priority-профиля и кнопки аудита.

### Фикс 0,0,0
- Если кэшированное тело не проходит проверку vtable — немедленный
  пере-резолв без троттла (раньше висело до смены мира).
- Бэкофф повторов 2с→8с→20с вместо постоянных 5с.

### Чистка лога
- «pawn read FAILED» — только при переходе в сбой или раз в 60с (было каждые 3с).

## Открыто (зафиксировано ранее)

- Выбор цели (кого бить в толпе) — штатный скоринг, не хранится в постоянном
  поле. См. SOURCE_OF_TRUTH §4.1. Глубокий RE сенсора — отдельная история.
