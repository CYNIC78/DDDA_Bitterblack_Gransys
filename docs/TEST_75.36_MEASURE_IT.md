# Замер 75.36 — померить то, что уже работает

Билд: `builds/46-errata-holds-20260820.zip`, тег **75.36-errata-holds**.

Фикс работает и держится: одна запись, 250 секунд без единого затирания.
Осталось получить ЧИСЛО вместо «вижу, что бьёт».

В прошлом прогоне гистограмма вышла пустой: `278 samples over 97 s, of them
0 in combat` — слежение было включено вне боя.

Что исправлено в билде: счётчик восстановлений больше не считает движение
ползунка за «игра затёрла». Появилась отдельная графа `hand changes`.

---

## Порядок

1. F12 → `Guardian Doctrine`. `Guardian fix` — снята, `inclination lever` — снят.
2. Галка **`Guardian dagger ban - LIFT IT`** — стоит.
3. Ползунок **`dagger AddS32` = 0**. Дождаться `APPLIED`.
4. DevTools → `GOAP probe` → `probe target` = own pawn → **`Goal codes`**.
5. **Найти врагов. Начать бой.**
6. **Уже в бою** включить **`code watch`**.
7. Драться, сколько получится. Нужно **хотя бы 500 боевых кадров**.
8. Выключить **`code watch`** — гистограмма печатается сама.
9. Прислать лог.

## Второй заход, если есть силы

10. Ползунок **`+2`**, снова пункты 5–8.

## Что должно получиться

Строка в гистограмме:

```
   54  WpnDaggerAtk    <frames>   <in combat>   <entries>
```

Сравнивать будем с эталоном из лога 17a: **500 боевых кадров, 0 кинжалов**.

Если в этом прогоне у кода 54 будет заметная доля — фикс доказан числом, и
эррату можно закрывать как готовую.

## Контрольные числа в панели

```
APPLIED (... = +0) | held 250 s | writes 1, re-asserts 0 (game overwrote us),
                                  2 hand changes, live slot 44
```

`re-asserts` должен остаться **нулём**. Если вдруг начнёт расти — скажи.
