# AUDIO_MUSIC_RECON — музыкальный слой (слой 0: сцена/атмосфера)

**Статус:** замысел согласован, кода нет, решения принимаются по протоколу R0→R4.
**Зачем документ:** зафиксировать рамку до первого кода (по `FIX_RULES`: сначала факты,
потом вмешательство). Если файл разойдётся с реальностью — править файл,
а не реальность (по `VISION`).

---

## 1. Замысел

Дать игроку свою музыку **в рантайме**, из папки мода, без правки файлов игры,
без хекс-редакторов и перепаковки arc. Движок остаётся дирижёром (когда играть,
переходы, громкости, катсцены) — мы меняем «пластинку», не дирижёра. Это прямое
продолжение философии `VISION.md` «меняем контекст, не решения» на аудио-домен.

**Почему это часть нашего мода, а не отдельный релиз:** только у нас есть живой
контекст (CombatBus, MonsterDirector, SpeciesCard, Nightmare-latch). Статичный
заменитель файлов этого не видит никогда. Развитие = вариант C (контекстный выбор).

**Рамка:** музыкальный модуль — «слой 0» (атмосфера). Не pawnai/monsterai код,
не правки AI/статов. Сам модуль живёт в `src/runtime/` (`src/audio/`),
конфиг — свой ini-sidecar, off-by-default, vanilla fallback при любой
неизвестности. Сейв неприкосновенен. Мод шипится **без музыки** — только
загрузчик + папка (юридическая чистота).

---

## 2. Подтверждённые факты

| Факт | Статус | Источник |
|---|---|---|
| BGM лежит loose-файлами: `nativePC\sound\stream\bgm\wave\*.sngw` | CONFIRMED (внешний) | музмоды Nexus, многолетняя практика |
| `.sngw` = OGG Vorbis (переименованный; играется VLC/Audacity) | CONFIRMED (внешний) | гайды по музмодам |
| Метаданные треков — `bgm.stq` внутри `nativePC\rom\bbs_rpg.arc` | CONFIRMED (внешний) | все музмоды таскают этот arc |
| **Размер файла должен ТОЧНО совпадать со значением в .stq, иначе краш** | CONFIRMED (внешний, пользователь) | опыт правки через hex |
| Файлы ББИ имеют **уникальные имена** (`Gazer.sngw`, `Shinigami_Battle.sngw`, `Demon_1`, `Lich_ZonbiDragon.sngw`…) | CONFIRMED (пользователь) | инвентаризация ресурсов |
| `No_***.sngw` — в основном Грансис (некоторые играют и на острове) | OBSERVED (пользователь) | — |
| `sSoundManager` и семейство `nSound*` присутствуют в TypeAtlas с DTI | CONFIRMED (репо) | `docs/generated/TYPE_ATLAS.md` |
| Формат `.stq` (STRQ) разобран: запись 24 B: имя/размер/сэмплы/каналы/луп | CONFIRMED (R0, оффлайн, якорь) | `docs/generated/STQ_FORMAT.md`, `tools/stq_dump.py` |
| Карта очереди: 125 треков игры + 2 титла (имена↔размеры↔длит.↔лупы) | CONFIRMED (R0) | `docs/generated/BGM_MAP.md` |
| Босс-сет ББИ на месте в таблице: `Demon_1`, `Shinigami_Battle`, `Gazer`, `Lich_DragonZonbi`, `Zako_1/2/3`, `DD_*` | CONFIRMED (R0) | все 6ch (5.1) |
| Титл: 48000 Гц, 6ch; стример терпим к битрейту Vorbis | 48k/6ch CONFIRMED; битрейт OBSERVED | — |
| Игра читает BGM через `CreateFileA` (ANSI), абсолютный путь `…\nativePC\sound\stream\…` | OBSERVED (R1, 84.70) | W-ветка чтения не используется |
| Каждый трек открывается ~3 раза подряд одним потоком (title, gamestart) | OBSERVED (R1) | подмена идемпотентна в окне 5 c |
| Строка `Tittle_DDN` встречается в памяти тысячами | OBSERVED (R1) | пробнику — отдельный `audio_probe.txt` |
| **STRQ-образ в памяти — СЫРОЙ: таблица `title.arc` читается движком как есть** | CONFIRMED (R1, 84.72) | `STRQ@0x…dbe0`, count=2, entry#0 `0x…dc1c`, namePtr=0x380; двойная находка (магия+якорь), адрес совпал |
| Перезапрос не предполагает ожидания пробника: 3 открытия титла идут мгновенно | OBSERVED (R1, 84.72) | первый вызов удерживается до валидации записи (гонка закрыта в 84.73) |
| Когда движок перечитывает размер из таблицы (каждый запрос vs раз на загрузку) | UNVALIDATED | ключевой вопрос R1/R2 |
| Параметры Vorbis, которые ждёт стример (rate/channels) | UNVALIDATED | R0/R2 |

---

## 3. Архитектура MVP (вариант A + обязательный патч размера)

**Конструктивно редирект и патч размера — ОДНА фича.** Редирект без патча =
крашгенератор (см. §2). Патч без редиректа бессмыслен.

```text
движок:    сцена → запрос трека (открывает *.sngw через CreateFile)
хук:       имя есть в нашем маппинге?
             нет  → пропускаем в оригинал (ванила/чужие музмоды не тронуты)
             да   → выбор из пула (RNG в MVP; в C — контекст)
                    ├─ CreateFile отдаём наш файл
                    └─ размер: GetFileSize(наш файл) → транзакционная запись
                       в запись .stq в памяти (validate → write → readback →
                       watch → rollback по канону ARCHITECTURE §4.2)
движок:    играет как обычно (луп, громкость, переходы — его забота)
```

Свойства:

- **Выбор — на каждый запрос трека**, не раз на сессию. Пулы работают «разная
  музыка на каждый заход к Деймону».
- Файлы в пуле могут быть **разной длины** — размер подставляется под выбранный
  файл в момент выбора. У статик-мода на ID один размер навсегда, у нас — по файлу.
- ini хранит **только имена** файлов. Размер — авто. Луп-точки — не раньше
  доказанной нужды (R2).
- Всё состояние — в памяти процесса и наших ini. Файлы игры и `bbs_rpg.arc`
  не трогаются вообще. Выход из игры = чистая ванила.

### 3.1 Иллюстративный контракт ini (не реализация)

```ini
[music]
enabled = 0

; одиночная подмена
map.Gazer            = music/bbi/gazer_theme.ogg

; RNG-пул: каждый заход — случайный из списка
map.Shinigami_Battle = music/bbi/death_1.ogg | music/bbi/death_2.ogg | music/bbi/death_3.ogg
```

### 3.2 Первая волна контента (scope MVP)

Остров ББИ, уникальные имена, дискретные боссы — удобный A/B стенд:

- Смерть (`Shinigami_Battle`)
- Прикованный циклоп
- Проклятый дракон (`Lich_ZonbiDragon`)
- Тёмный епископ
- Глаз (`Gazer`)
- Деймон (`Demon_1`, …)
- Сет из 3 боевых треков острова по тиру сложности

#### 3.2.1 Конкретный пул (20 каверов → 13 ключей; работа→маппинг)

| Ключ STRQ | Оригинал: длит. / луп, c | Пул каверов (mp3 → ogg 48k/6ch) |
|---|---|---|
| `bgm\wave2\Tittle_DDN` | 87,5, луп 57,0→85,0 | tittleddn, tittleddn1 (+ уже готов Tittle.ogg) |
| `bgm\wave\Demon_1` | 223,5, 54,4→222,0 | demon1_instrumental, demon1_instrumental1 |
| `bgm\wave\Shinigami_Battle` | 115,7, 12,7→112,6 | shinigamibattle, shinigamibattle2 |
| `bgm\wave\ShuuzinCycrops` | 132,6, 13,4→125,9 | shuuzincycrops, shuuzincycrops1 |
| `bgm\wave\Gazer` | 126,8, 24,3→122,3 | **gazer, gazer1, gazer2** (пул из 3) |
| `bgm\wave\Lich_DragonZonbi` | 110,6, 3,2→108,2 | lichdragonzonbiextended3 |
| `bgm\wave\Upper_ZakoDragon` | 88,9, 1,7→87,2 | upperzakodragon, upperzakodragon1 |
| `Zako_1` / `Zako_2` / `Zako_3` | 110,0, луп 5,0→107,0 | zakocombined → разбить на 3 |
| `bgm\wave\DD_Entrance` | 63,7, one-shot | ddentrance |
| `bgm\wave\DD_Kasou` | 130,2, 26,0→124,0 | ddkasou |
| `bgm\wave\No_018` | 55,2, one-shot | no018 (тема Кассардиса) |
| `bgm\wave\No_046` | 147,6, 5,0→146,1 | no046, no046 1 (редактор персонажа) |

Весь сет — 48 кГц / 6ch (шаблон Tittle.ogg). Имена каверов свободны;
при конвертации чистить от служебного мусора (`[[usesuno.com]]`,
хвостовые пробелы).

Режим: **rotation/shuffle на границе трека** (выбор при каждом запросе
движка). FSM/смена посреди трека — сознательно НЕ делаем: прервать
играющий трек в варианте A/C нельзя. Естественные границы (вход в бой,
возможная перезапросная смена фазы у босса, победа/выход в меню) дают
достаточно точек выбора. «Перезапрашивает ли движок музыку между фазами»
— R1 вопрос №4.

Тёмный епископ: РЕШЕНО — ключ `bgm\wave\Lich_DragonZonbi` на весь
бой целиком (у Епископа один трек; его дракон — особый вариант: без
фиолетового кристалла в сердце, не летает). Кавер `lichdragonzonbiext3`.
Семантика ключей ББИ (репортаж пользователя, OBSERVED ~99%; триггеры
подтвердятся на R1 живым логом): `Lich_DragonZonbi` = Епископ;
`Upper_ZakoDragon` = боевая тема Проклятого Дракона (кавер в пуле);
`Shitaiwaki` (死体湧き) = тема спавна/подхода некрофагов (Гармы и пр.;
встреча с Проклятым Драконом = спавн под Shitaiwaki → бой под
Upper_ZakoDragon). Контрольная сверка формата: байты .stq = байты
диска на живой установке (7 705 703 B ✓).
Побочный факт для R1 №4: один ключ на бой в две фазы ⇒ вероятно,
перезапросов между фазами нет — увидеть в живом логе.
Пробелы без каверов: `Shitaiwaki` (нужен трек некрофагов),
`DD_Jousou`/`DD_Chuusou`, `Demon_2+` если есть.

### 3.3 Авторский канон первой волны (контент-замысел)

- **Лейтмотив:** хор «испорченных пешек» — под властью чёрной магии острова
  пешки «подпевают» монстрам на арене, как оккультная группа поддержки.
  Исполнение натужное: подчиняются чужой воле, сопротивляться не могут,
  и понимают инверсию своей миссии.
- **Резонанс с механикой мода:** та же инверсия, что в Nightmare-ярме
  (пешка пала → встала чужой). Слой 0 доносит историю, которую слой 1
  показывает геймплеем. Нарратив без квестов, катсцен и текста.
- **Миксы:** оркестровый «голливудский» и тяжёлый (metal + symphonic).
  Оба живут в пулах ini одновременно — разные размеры допустимы
  (размер подставляется под выбранный файл, §3).
- **Язык хора:** латынь (ритуальная глубина) и/или английский (игрок
  улавливает сюжетную подкладку). Открыто; система не ограничивает —
  разные языковые версии = разные позиции пулов.
- **Эскалация хора по тирам:** бой 1-го тира — далёкий шёпот,
  2-го — распознаваемый чант, боссы/3-й — полный хор.
- **Будущее правило варианта C:** при активном Nightmare-latch одержимой
  пешки — принудительно «хор-вариант» боевого трека: твоя пешка поёт им.
- **Будущий кавер `Shitaiwaki` (концепт):** замысел «незамеченная
  угроза» — не боевая тема, а стелс-тревога: холодные пады, скрипы,
  полудохшее дыхание, далёкие глотки — музыка как СЕНСОР мира: игрок
  понимает «что-то большое рядом, меня ещё не видят» и получает
  шанс тихо свалить. Звуковые эффекты (шаги/шерех) — часть лора трека.

---

## 3.4 Канон хранения и конвертации (решено, стартовый срез — титл)

Первый практический срез: **титл-трек** `wave2\Tittle_DDN.sngw`
(грузится сразу после splash, дискретный, легко проверяемый).
Намёк извне: Nexus-моды на титл таскают ещё и `rom\title.arc` — размерная
запись титла с высокой вероятностью в `title.arc`, а не в `bbs_rpg.arc`
(проверить в R0 якорём по известному байт-размеру).

**Хранение:** `<игра>\DDDA_AI_Overhaul\music\…` — каноничная папка мода
(`src/ModPaths.h`: ленивая инициализация, путь от EXE, каталог создаётся
автоматически). Имена наших файлов свободны; оригинальное имя файла —
только ключ в ini-маппинге. Наши файлы — с расширением `.ogg`, чтобы
визуально отличать контент пользователя от игровых имён.

Ини карты хранится **внутри `music\`**: `DDDA_AI_Overhaul\music\
ddda_music_map.ini`; пути в нём — относительно самой папки `music\`.
Подпапки (`title\`, `bbi\`, `gransys\`, …) свободны для
переименования при условии правки путей в ini. Папка `music\`
самодостаточна: конфиг с контентом, удаление = чистая деинсталляция.

**Конвертация (спек):**
- формат: OGG Vorbis, качество высокое (q 9–10; оригиналы ≈660 кбит/с на 6ch);
- частота = у оригинала. **CONFIRMED для титла: 48000 Гц, 6ch (5.1)** —
  не 44100; каждый оригинал проверять отдельно;
- **CONFIRMED-якорь титла:** размер `Tittle_DDN.sngw` = 5 327 566 байт
  = `0x00514ACE` → в .stq ищем LE-последовательность `CE 4A 51 00`.
  Длит. ≈64–65 c при ~660 кбит/с;
- **каналы = у оригинала. Обязательно.** Подмена 6→2 или 2→6 = краш
  (стример считает буферы по каналам). Проверка: MediaInfo / VLC / Audacity;
- длительность и размер — свободны (размер патчит система);
- громкость: без клиппинга, потолок ~−1 dBTP; мастер-громкость — движка;
- лупящимся трекам — незаметный стык (титл лупится).

**Если оригинал 5.1, а мастер стерео:** апаем в Reaper до 6 каналов
(порядок Vorbis 5.1: L R C LFE Ls Rs): стерео на фронт, реверб в тыл,
центр/LFE по вкусу/пустые.

**Будущая валидация загрузчика (не MVP):** сравнивать каналы/частоту
замены с оригиналом; несовпадение → отказ в ванилу + строка в лог.

---

## 4. План B: паддинг вместо патча памяти

Если live-запись в .stq окажется недостижимой (таблица не находится / движок
кэширует размер на загрузке и не перечитывает): инструмент конвертации **дополняет
OGG-пользователя до точного размера из .stq** валидным контентом контейнера
(VorbisComment произвольной длины; точность до байта). Тогда хватит чистого
редиректа без единого байта записи в память. Цена: файлы раздуваются, пул
должен быть выровнен под один размер. Не цель — страховка.

## 5. Открытые вопросы (в порядке протокола)

R0 снял вопросы таблицы (раскладка, карта имён, 48 кГц/6ch титла) и
«wave2» (там только титл; весь игровой BGM — `wave/`). Остались живые:

1. Где физически лежит размер после загрузки .stq: сырой буфер ресурса
   или распарсенные поля в `sSoundManager`-иерархии? (старт: DTI `sSoundManager`)
2. Перечитывает ли движок размер **на каждый запуск трека**? Если кэширует
   однажды — переключение размера «на лету» невозможно, пулы = одинаковый
   размер (или план B).
3. Луп-политика подмены: пересчёт `+0x10/+0x14` пропорционально новой
   длительности или «луп на весь файл минус хвост» (титл: оригинал лупит
   57→85 c из 87,5 c). Решается A/B на R2.
4. Точная карта триггер↔трек на острове (таблица есть — `BGM_MAP`;
   осталось увидеть запросы в живую, R1).
5. Параметры Vorbis для стримера: битрейт свободен (OBSERVED), частота
   должна матчить оригинал (48 кГц подтверждено на титле).
6. Открывает ли движок файл разово или подцикливает хэндл; есть ли
   кэш меню/титров.

---

## 6. Протокол

- **R0 (оффлайн, без игры): ЗАВЕРШЁН.** Формат STRQ вскрыт якорным методом,
  карта 127 записей извлечена, парсер в `tools/stq_dump.py`.
  Выходы: `docs/generated/STQ_FORMAT.md`, `docs/generated/BGM_MAP.md`.
- **R1 (read-only, игра): ПРИБОР ГОТОВ (код 84.69).** Модуль
  `src/audio/AudioRedirect.{h,cpp}`: хук CreateFile W/A (дешёвый фильтр
  `.sngw`), журнал обращений (путь/флаги/поток), разовый пробник
  STRQ-образов в памяти (валидация по магии + имени титла == «сырой
  ресурс в памяти»). Пишет только в наш лог, игру не трогает.
  Тумблер R2 (enabled=1) уже встроен: подмена файла + транзакционный
  патч 4 полей записи + readback + WATCH(2.5с) + rollback в Shutdown.
- **R1 (read-only, игра):** пассивный лог обращений к `sound\stream\bgm\wave`
  (какой файл, когда, с какого потока). Никакой записи. Подтверждение §2/§5.
- **R2 (MVP):** хук + выбор + запись размера транзакционно (`FIX_RULES` §4б:
  write → readback → WATCH 2–3 с → rollback). Приёмка: заданный трек ББИ
  заменяется, без краша, луп ровный, после боя — обычная музыка; отключение
  модуля без перезапуска = ванила.
- **R3:** развитие маппинга до полной первой волны (§3.2) + конвертер
  Suno→ogg (отдельная тулза или инструкция; ffmpeg).
- **R4 (вариант C):** контекстный выбор через CombatBus: вид босса из
  SpeciesCard/бестиария, ночь/день, остров/материк, Nightmare-latch.
  Правила — в ini (веса/условия). Только после подтверждённого R2.

Стингеры посреди играющего трека (напр. на «срыв темпа» из `VISION`) —
потребность варианта B (свой миксер). Не цель текущего трека; фиксируем,
чтобы не обещать лишнего.

---

## 7. Риск-реестр

| Риск | Митигация |
|---|---|
| Размер кэшируется на загрузке → разные размеры в пуле невозможны | R2 A/B; план B (паддинг) |
| Запись в .stq-таблицу не находится | план B; деградация в «подмена только равных размеров» |
| Хук на аудио-потоке | в detour — ничего тяжёлого; пути резолвим заранее, без локов и логов |
| Кривые параметры ogg юзера | конвертер + валидация при загрузке (отказ → ванила, строка в лог) |
| Ожидание «динамической музыки AAA» | рамка зафиксирована выбором на границе трека |

## 8. Границы (явно)

- Не трогаем AI, статы, баланс, сейв — отдельный модуль, отдельный ini.
- Не шипим музыку в моде/репо; только загрузчик и схема конфига.
- Не обещаем интерактивные стингеры до варианта B.
- Не ломаем чужие музмоды: нет нашего маппинга — файл проходит как есть.

---

## 9. Связанные документы

| Документ | Отношение |
|---|---|
| `docs/VISION.md` | философия «контекст, не решения»; срыв темпа — будущий аудио-повод |
| `docs/ARCHITECTURE.md` | слои LIVE/CATALOG/MEMORY; транзакции §4.2 |
| `docs/FIX_RULES.md` | протокол вмешательства, правило «голоса у записи» |
| `docs/generated/TYPE_ATLAS.md` | `sSoundManager`, семейство `nSound*` |
| `docs/SOURCE_OF_TRUTH.md` | сюда пойдут подтверждённые аудио-контракты после R1/R2 |

## Addendum R2 (билд 84.75-loop-hunt)

- **Подмена в бою состоялась**: `SUB … -> title\tittleddn_a.ogg (9079881B,
  11280384 smp, 6ch)`, четыре PATCH [ok], WATCH ok. Guest-tema в меню.
- Открытый факт: игра лупит РАНЬШЕ конца файла, хотя +0x10/+0x14 пропатчены
  на полный трек (0..11280384). Гипотеза: плеер слушает луп-точки из
  распарсенных структур, построенных при загрузке title.arc до нашего
  патча; сырая таблица — источник лишь для размера/сэмплов.
- Ответный ход: `[music.loops]` в ini (в сэмплах, из редактора); дамп всех
  записей таблицы; LOOPHUNT — поиск пары (2734966, 4078967) вне таблицы.
- Семантика задержки: пробник/охотник одноразовы за процесс; постоянных
  просадок нет — дергается только загрузка меню (~2 с).
## Addendum A/B (84.81–84.82)

- Гипотеза «лупы из vorbis-комментариев» ОПРОВЕРГНУТА: LOOPSTART=7000000 в
  файле луп не сдвинул. Теги .sngw носитель информативный (совпадают со
  STRQ), но не исполняемый. Арбитр лупов = распарсенные копии STRQ.
- Пайплайн титла (канон): патч 4 полей сырой таблицы + LOOPSTRIKE/FIELD
  в живых копиях + [music.loops] для искусства. shine: файл с тегами можно
  продолжать делать для совместимости с внешним софтом, движку они безразличны.
