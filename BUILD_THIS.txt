Build 52 — Main Pawn semantic map: all planner slots (read-only)

Собрать:
  Visual Studio → Release | Win32 → Build Solution

Проверить в F12:
  build 52-pawn-semantic-all-slots

Build 51 подтвердил:
  - exact bow action: cPlActWpnBow / packed 0x02060000 / priority code 57;
  - code 57 PlanCtrl -> WpnBowAtk2 IDs 12/107 + DmgUkemi 149 fallback;
  - exact dagger action: cPlActWpnDaggerAtckLandL / code 54;
  - code 54 -> WpnDaggerAtk IDs 9/124/132/133/10;
  - current combat target: uCmc+0x2EB8;
  - cAICtrl child layout подписан.

Цель Build 52:
  - не ловить каждый priority code вручную;
  - за один snapshot пройти все 91 cPlanCtrl slots;
  - для каждого slot собрать cGoalPlanningNode+0x04 links;
  - сопоставить links с 69 rAIGoalPlanning resources и Interface IDs;
  - массово заполнить pawn_priority_semantics.json/csv.

Добавлен JSON section:
  plannerSlots[91]
    code
    PlanCtrl ptr/hash
    nodeLinks[]

Analyzer автоматически переводит каждый node link в:
  GOAP resource path
  InterfaceID
  cCmc action-interface pointer

ТЕСТ — ОДИН снимок

1. Только Arisen + Main Pawn.
2. Priority profile = green vanilla.
3. Загрузить сейв и подождать 20 секунд, чтобы AI resources были полностью готовы.
4. F12 → Player + Main Pawn recon.
5. Нажать "Find both + capture baseline".
6. Дождаться AI bridge 001.

Прислать:
  ddda_pawn_ai_bridge_001m.json

Состояние пешки не критично: Build 52 читает все planner slots, а не только selected.
Build 52 read-only. F9 не использовать.
