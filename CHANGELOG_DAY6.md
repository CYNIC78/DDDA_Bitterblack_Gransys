# День 6 — 14.08.2026: гоблин вскрыт, формат ассетов известен

## Главное

Формат **XFS** (MT Framework property container) оказался самоописывающимся:
внутри каждого файла лежит таблица классов со всеми именами полей, типами и
размерами. Имена — **внутренние японские подписи Capcom**, не обфусцированы.

Это не «нашли какие-то байты». Это официальная спецификация из самих файлов игры.

## Что добавлено

### Инструменты (`tools/`)

| файл | что делает |
|---|---|
| `xfs_dump.py` | схема + значения `.prp` / `.shl` / `.ltg`. Флаги `--values`, `--json` |
| `rst_dump.py` | `maxHP` + пороги нокдауна/флинча из `.rst` |
| `lmt_dump.py` | мотионы: id, число кадров, длительность, loop |

### Документация

- `docs/ASSET_FORMATS.md` — полная спека (§1–18)
- `docs/FIELD_MAP.md` — дописаны HP и поправка про пул сбивания

## Ключевые находки

### maxHP найдено и проверено

| враг | maxHP | нокдаун | флинч |
|---|---:|---:|---:|
| Goblin `em0100` | 1000 | 100 | 100 |
| Hobgoblin `em0101` | 2000 | 650 / 500 | 450 / 300 |
| Grimgoblin `em0102` | 6000 | 2200 / 800 | 1500 / 500 |

Сверено с вики сообщества — **точное совпадение**. ATK/DEF/MATK/MDEF/WT из
`_cmn.prp` тоже сошлись. Два независимых источника.

### 72 поля боевых характеристик

`em0100_cmn.prp`, `sizeof=320`, hash `0x7E509FE9`. Сопротивления делятся на
**множители урона** (7 штук, ~1.0) и **пороги дебафов** (20 штук, сотни/тысячи).

### Сенсоры

`em0100A.sn2`: зрение = конус **60°**, дальность **1500**, сустав головы.
Ещё 4 сферы 360°. Агро-радиус больше не магия.

### Поправка: «смерть при HP 61/80/15» — это не HP

Все три числа < 100 = **пул сбивания** (`.rst` +0x04/+0x0C), не HP (1000).
Восстановление `0.3` объясняет «падает после смерти». «Испарение адреса» —
гистерезис выгрузки (карта 4) + смена адресов тел между сессиями.

**Следствие:** не ждать `hp <= 0`. 67 занятых слотов в `e0100_dm.lmt` из 173
выбираются переменной состояния, а не сравнением float с нулём.

## Открыто

- current HP внутри 29-КБ тела — искать float `1000.0`, проверка по
  `2000.0` / `6000.0` у сородичей
- флаг/номер состояния смерти — A/B «живой → раненый → труп», не отходя
- живой `cThinkMgr` (фаза 3.1) — без него нет вмешательства в поведение

## Полки

| находка | полка |
|---|---|
| схема 72 полей, сенсоры, дельта видов | CATALOG |
| сигнатуры для скана, точки `.ltg` | LIVE |
| правка `.eap` на диске | PACK — не сейчас |

Ни одна находка не требует подмены `game_main.arc`.
